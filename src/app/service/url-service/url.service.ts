import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class UrlService {

  constructor() { }

  addURL(title: string, url: string, method: string, contentType: string, body: string, period: number) {
    
  }
}
